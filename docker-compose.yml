services:
  go_app:
    build:
      context: ./docker/
      tags:
        - "jaaywill4/go_server:latest"
    container_name: go_app
    image: jaaywill4/go_server:latest
    restart: always
    expose:
      - 8080
    ports:
      - 8080:8080
